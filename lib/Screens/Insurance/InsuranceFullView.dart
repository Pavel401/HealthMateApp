import 'package:flutter/material.dart';

class InsuranceFullView extends StatelessWidget {
  const InsuranceFullView({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [],
    );
  }
}
